[package]
name = "ph-audit"
version = "0.1.0"
edition = "2021"
description = "Tamper-evident, hash-chained audit log for the PH Press editorial system."
publish = false

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
